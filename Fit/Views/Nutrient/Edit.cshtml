@model Fit.ViewModels.Nutrient.NutrientEditViewModel;
@{
    ViewBag.Title = "Wijzig Voedingsstof";
    Layout = "_Layout";
}
<div class="card">
    <div class="card-body">
        <form class="form" asp-controller="Nutrient" asp-action="Edit">         
            <div class="form-row">
                <div class="form-group col-md-6">
                    <label asp-for="@Model.Name">Naam</label>
                    <input type="text" class="form-control" placeholder="Voedingstof naam" asp-for="@Model.Name" value="@Model.Name" required>
                </div>
                <div class="form-group col-md-6">
                    <label asp-for="@Model.MaxIntake">Maximale inname</label>
                    @* <input asp-for="@Model.MaxIntake" value="@Model.MaxIntake" class="form-control" type="number" placeholder="0.00" required min="0"  step="0.01" pattern="^\d+(?:\.\d{1,2})?$"  *@
                    @*        onblur="this.parentNode.parentNode.style.backgroundColor=/^\d+(?:\.\d{1,2})?$/.test(this.value)?'inherit':'red' "> *@
                    <input type="number" step=".01" class="form-control" placeholder="Aantal grammen" asp-for="@Model.MaxIntake" value="@Model.MaxIntake" required>
                </div>         
            </div>                         
            <button type="submit" class="btn btn-success">Voeg Toe</button>
        </form>
    </div>
</div>